// lemnity-box-canvas-editor.tsx — координаты вставки zero-block
export const ZERO_BLOCK_BASE_TOP_PX = 120;
export const ZERO_BLOCK_STEP_Y_PX = 28;
export const ZERO_BLOCK_BASE_LEFT_PX = 32;
export const ZERO_BLOCK_STEP_X_PX = 14;
export const ZERO_BLOCK_COLUMNS = 5;

// build-share-popover.tsx — задержка сброса статуса "ссылка скопирована"
export const LINK_COPIED_RESET_MS = 3000;

// lemnity-box-section-width-grid.ts — дефолтное количество колонок сетки
export const DEFAULT_GRID_COLUMNS = 12;

/** Горизонтальные поля 12-колоночной сетки на холсте Box (пунктир и фон колонок). */
export const LEMNITY_GRID_OUTSIDE_GUTTER_PX = 50;

// Zero Block configurable grid defaults
export const ZB_GRID_DEFAULT_COLUMNS = 12;
export const ZB_GRID_DEFAULT_MARGIN_PX = 40;
/** 0 = matches standard page grid (no inter-column gutter). */
export const ZB_GRID_DEFAULT_GUTTER_PX = 0;

// Общие тайм-ауты API
export const SANDBOX_PATCH_TIMEOUT_MS = 120_000;
export const WEBHOOK_DISPATCH_TIMEOUT_MS = 10_000;
