"use client";

import type { SlideTheme } from "@/lib/slide-graph/types";

interface Props {
  theme: SlideTheme;
  projectId: string;
}

export function ThemeEditorPanel(_: Props) {
  return <div>Theme Editor</div>;
}
