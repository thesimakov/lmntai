/** Реэкспорт overlay-хелперов Canvas (iframe): слои hover/selection и подписи. */
export {
  createOverlayController,
  ensureOverlayRoot,
  ensureOverlayStyles,
  removeOverlayRoot,
  LEMNITY_OVERLAY_ROOT_ID
} from "@/lib/editor/canvas-overlay";
