# Если при сохранении макета (PATCH HTML) видите 413, запрос может отрезаться ДО Next.js или до lemnity-builder.
# Клиент отправляет HTML с Content-Encoding: gzip (меньший размер на проводе — чаще помогает против лимита nginx).
# Проверьте лимиты тела запроса и перезапустите прокси + pm2 после правок.

# --- Nginx (server или location для фронта) ---
client_max_body_size 64m;

# --- Caddy (в блоке сайта или handle для /api/*; синтаксис зависит от версии — см. docs) ---
# Примерная идея: разрешить крупное тело для маршрута к приложению.
# limits body 64MiB   # уточните под вашу версию Caddy
